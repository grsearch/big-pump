'use client';
import ShadowPanel from './shadow-panel';


const names:Record<string,string>={sentiment:'讨论态度',narrative:'叙事清晰度',substance:'论据具体程度',community:'观点参与度',manipulationRisk:'操纵话术风险 ↑'};
export function AssessmentCard({value}:any){if(!value)return <div className="empty-note">尚无 AI 分析。配置供应商后，有效讨论达到 10 帖、5 位作者才进行分析。</div>;if(value.status!=='ok')return <div className="detail-box">{value.status==='pending'?'AI 分析中':value.summary??'等待有效样本'}</div>;return <div className="assessment"><div className="score-summary"><strong>{value.overall??'—'}<small>/ 100</small></strong><div><b>讨论评价指数</b><p className="muted">{value.sample?.posts} 帖 · {value.sample?.authors} 位作者 · 证据充分度 {Math.round(value.confidence*100)}%</p></div></div><p className="muted">这是样本评价，不是上涨概率；高风险维度会扣分。{Date.now()-value.at>600000?'分析已超过 10 分钟，不能用于新开仓。':''}</p><div className="dimension-grid">{Object.entries(names).map(([key,label])=><div key={key}><label>{label}<b>{value.dimensions[key].score??'未知'}</b></label><div className="progress"><div style={{width:(value.dimensions[key].score??0)+'%',background:key==='manipulationRisk'?'#e7bb73':'#77e6b2'}}/></div><p>{value.dimensions[key].reason}</p><small>证据：{value.dimensions[key].evidenceIds.map((id:string)=><a key={id} href={'https://x.com/i/status/'+id} target="_blank" rel="noreferrer">{id.slice(-6)} ↗ </a>)}</small></div>)}</div><div className="detail-box"><b>Quote 叙事匹配：{value.quoteMatch?.score??'未知'}{value.quoteMatch?.score!=null?' / 100':''}</b><p>{value.quoteMatch?.reason??'等待新版本分析'}</p><small>独立研究指标，不参与总分或买入条件。{value.quoteMatch?.evidenceIds?.map((id:string)=><a key={id} href={'https://x.com/i/status/'+id} target="_blank" rel="noreferrer"> 证据 ↗ </a>)}</small></div><p>{value.summary}</p><small className="muted">{value.provider} / {value.model} · {new Date(value.at).toLocaleString('zh-CN')} · {value.promptVersion}</small></div>;}
export default function ResearchPanel({view,data,demo,online,busy,act}:any){const ai=data?.analysisStatus;if(view!=='AI 评价')return <ShadowPanel {...{data,demo,online,busy,act}}/>;return <><div className="section-intro"><h2>先过滤播报，再阅读讨论</h2><p>模板行情、重复喊单不计入有效讨论；推广内容单独降权保留。负面观点不因情绪被删除。</p></div><div className="notice">{ai?.configured?'供应商已配置':'AI 密钥未配置'} · {ai?.enabled?'自动分析已开启':'自动分析关闭'} · {ai?.provider??'deepseek'} / {ai?.model??'deepseek-flash'} · 今日 {ai?.callsToday??0} / {ai?.maxCalls??100} 次请求</div><div className="panel"><div className="panel-head"><h2>代币评价</h2><span>每币至少间隔 5 分钟；相同样本不重复请求</span></div>{(data?.assessments??[]).map((a:any)=><details className="ai-detail" key={a.ca}><summary>{data?.tokens?.find((t:any)=>t.ca===a.ca)?.symbol??a.ca} <span>{a.status==='ok'?`${a.overall??'未知'} / 100`:a.status==='insufficient'?'样本不足':a.status==='error'?'分析失败':'分析中'}</span></summary><AssessmentCard value={a}/></details>)}{!data?.assessments?.length&&<div className="empty-note">没有分析结果。样本不足、接口失败或未配置时不会伪造分数。</div>}</div></>;}