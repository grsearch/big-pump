# 服务器更新入口

“更新代码，按最新代码执行”指部署本次新增改动，不表示依次重跑 deploy 目录中的所有历史迁移和维护步骤。先检查实际版本差异、运行状态和现有配置，再执行必要检查与部署。

## 普通更新

实盘C当前退出规则见 [固定止损-20%部署说明](live-stop20.md)：+40%激活、10%回撤、30分钟上限，并新增-20%固定止损；旧文档中无固定止损的说明不再适用于当前版本。

- 只执行本次变更相关的测试、构建和服务更新。纯文档或独立维护脚本变更不要求重启 collector / dashboard。
- 已指向 `scripts/export_sharded.py` 且 `Restart=no` 的导出服务，保留原 Python、凭证环境、DATA_DIR 和北京时间 06:00 调度；不重复安装、停用或触发导出。入口及导出行为未变时，不重复做大库快照或上传验证。
- 不因更新代码自动运行 `prune_wallet_audit.py`、历史补导出或服务器自建的 prune-chain 脚本。每日导出与历史审计清理是独立任务。
- 不删除数据库、WAL、SHM，不自动 VACUUM。SQLite 删除后的空闲页可供后续写入复用，文件大小不能作为清理是否成功的依据。

## 已完成的一次性维护

用户提供的 2026-09-13 17:07:49 服务器日志显示，本轮 W1 / W2 / W3 历史 wallet 审计清理链已经结束。普通更新不要再运行该轮 dry-run 或 apply；详情见 [钱包审计维护记录](wallet-audit-retention.md)。

需要新的历史清理时，另行明确范围并保留归档验证，不复用“更新代码”作为执行授权。发现已有维护进程，先看进度与退出状态，不重复启动。OpenClaw 请求超时不代表后台任务已停止。
